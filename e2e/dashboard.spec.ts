import { test } from '@playwright/test';

test.describe('Dashboard', () => {
  test('updates URL when Start/End dates change (shallow)', () => {
    test.skip();
  });
  test('WeeklySummary shows entries / skills / emotions counts', () => {
    test.skip();
  });
  test('EmotionChart (Chart.js) renders after at least one entry', () => {
    test.skip();
  });
  test('SkillsFrequency (Chart.js) renders after at least one entry', () => {
    test.skip();
  });
  test('UrgeHeatmap renders grid cells for the selected range', () => {
    test.skip();
  });
});
