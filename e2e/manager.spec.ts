import { test } from '@playwright/test';

test.describe('Manager Views', () => {
  test('Manager sees only assigned users and their summaries', () => {
    test.skip();
  });
  test('Manager cannot access other managers’ users', () => {
    test.skip();
  });
});
