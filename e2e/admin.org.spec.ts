import { test } from '@playwright/test';

test.describe('Admin Org', () => {
  test('Admin can see member list', () => {
    test.skip();
  });
  test('Admin can change role USER ↔ MANAGER', () => {
    test.skip();
  });
  test('Admin can assign manager for USER', () => {
    test.skip();
  });
  test('Aggregates (Managers/Users 7d summaries) render', () => {
    test.skip();
  });
});
