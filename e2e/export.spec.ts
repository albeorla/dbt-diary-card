import { test } from '@playwright/test';

test.describe('Export', () => {
  test('History export CSV triggers a file download', () => {
    test.skip();
  });
  test('Print export page renders entries for default 30 days', () => {
    test.skip();
  });
});
