import { test } from '@playwright/test';

test.describe('Calendar', () => {
  test('prev/next month navigation updates the grid', () => {
    test.skip();
  });
  test('Today button jumps to current month', () => {
    test.skip();
  });
  test('Arrow keys move focus in the grid', () => {
    test.skip();
  });
  test('Enter/Space opens quick-entry modal for focused date', () => {
    test.skip();
  });
});
